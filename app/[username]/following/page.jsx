const { default: Following } = require("./Following")

export const metadata = {
    title: 'Following',
}

const FollowingPage = () => {
  return (
    <Following />
  )
}
export default FollowingPage